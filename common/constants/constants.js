export { keyboardMapping } from './keyboard-mapping';

export const ClassNames = {
  PRESSED: 'pressed',
}

export const EventNames = {
  MOUSEUP: 'mouseup',
  MOUSEDOWN: 'mousedown',
  KEYUP: 'keyup',
  KEYDOWN: 'keydown',
}

export const TagNames = {
  DIV: 'div',
  BUTTON: 'button',
}

export const Devices = {
  MOUSE: 'mouse',
  KEYBOARD: 'keyboard',
}